# Phaser prototype assets

Place the Unity-exported background image here as `main-scene-background.png` to display it in the Phaser prototype. The file is ignored by Git so you can manage it locally without committing large binaries.
