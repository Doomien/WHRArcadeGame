# Phaser Source Placeholder

An attempt was made to clone the official Phaser repository (`https://github.com/photonstorm/phaser`) into this directory, but it failed because outbound HTTPS access is blocked in the current environment (`fatal: unable to access ... CONNECT tunnel failed, response 403`).

When network access is available, run the following command from the `WHRArcadeGame` root to populate this folder with the Phaser source:

```bash
git clone https://github.com/photonstorm/phaser.git Phaser
```

Alternatively, download a release archive from the [Phaser releases page](https://github.com/photonstorm/phaser/releases) and extract it here.
